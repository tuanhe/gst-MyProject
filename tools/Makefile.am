bin_PROGRAMS = gst-MyProject

# sources used to compile this plug-in
gst_MyProject_SOURCES = gstMyProject.c

# compiler and linker flags used to compile the program, set in configure.ac
gst_MyProject_CFLAGS = $(GST_CFLAGS)
gst_MyProject_LDADD = $(GST_LIBS)

